import React, { useState } from 'react';
import { useCoop } from '../context/CoopContext';
// TEMP: arquivo sera atualizado em seguida com a versao completa
export const RhView: React.FC = () => {
  return <div className="p-6">Carregando modulo RH...</div>;
};
